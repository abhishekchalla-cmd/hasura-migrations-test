table:
  name: states
  schema: public
object_relationships:
  - name: country
    using:
      foreign_key_constraint_on: country_id
array_relationships:
  - name: addresses
    using:
      foreign_key_constraint_on:
        column: state_id
        table:
          name: addresses
          schema: public
