table:
  name: countries
  schema: public
array_relationships:
  - name: addresses
    using:
      foreign_key_constraint_on:
        column: country_id
        table:
          name: addresses
          schema: public
  - name: states
    using:
      foreign_key_constraint_on:
        column: country_id
        table:
          name: states
          schema: public
